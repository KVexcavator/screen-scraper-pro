так у меня работает
```ui/components/app.slint
// https://docs.slint.dev/latest/docs/slint/
// for preview palitra: Slint: Show Preview
import { Menu } from "./header/menu.slint";
import { BodyView } from "./content/body.slint";
import { Button, ListView, VerticalBox } from "std-widgets.slint";

export component AppWindow inherits Window {
    width: 800px;
    height: 600px;
    title: "GAME APP";

    in property <[string]> titles;
    callback request_titles();
    callback window_selected(index: int);

    property <bool> show_modal: false;

    VerticalBox {
       Button {
           text: "Select Window";
           clicked => {
                root.request_titles();
                root.show_modal = true;
           }
       }
    }

   if show_modal : ListView {
        for title[i] in titles: Button {
            text: title;
            clicked => {
                root.window_selected(i);
                root.show_modal = false;
            }
        }
   }


//    VerticalLayout {
//        spacing: 0px;
//        Menu {}
//        BodyView {}
//    }
}
```
```ui/components/header/menu.slint
import { ComboBox } from "std-widgets.slint";

export component Menu inherits Rectangle {
    height: 44px;
    background: #3a3a3a;

    HorizontalLayout {
        padding-left: 12px;
        padding-right: 12px;
        spacing: 12px;

        Rectangle { width: 1px; }

        Text {
            text: "SOME TEXT";
            color:  #ff9800;
            font-size: 18px;
            vertical-alignment: center;
        }
    }

}
```
```ui/components/content/body.slint
export component BodyView inherits VerticalLayout {

}
```
1. хочу что бы кнопка была в меню, модалка в body
2. после выбора, модалка со списком дожна исчезать
(гдобал н еисползовать ни вкоем случее, не работтает эта муйта)