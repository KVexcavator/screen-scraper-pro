export component AppHeader inherits Rectangle {
    height: 44px;
    background: #3a3a3a;

    Text {
        text: "GAME APP";
        color: #ff9800;
        font-size: 20px;
        font-weight: 600;
        vertical-alignment: center;
        horizontal-alignment: center;
    }
}